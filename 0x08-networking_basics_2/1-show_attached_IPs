#!/usr/bin/env bash
# This script displays all active IPv4 IPs on the machine

# Use ifconfig to get IPv4 addresses and filter out only active interfaces
ipv4_addresses=$(ifconfig | grep -oP '(?<=inet )(\d+\.\d+\.\d+\.\d+)' | grep -v '127.0.0.1')

# Print the IPv4 addresses
echo "$ipv4_addresses"

